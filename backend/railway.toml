[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn mitra_api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
